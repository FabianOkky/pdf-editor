# Project Status Board

> **This is the live cross-phase board — the single source of truth for "what is done".**
> Every session MUST update this file before finishing. Keep it honest: the next session
> trusts it blindly.

Legend: ⬜ Not started · 🟦 In progress · ✅ Done · ⚠️ Blocked

_Last updated: 2026-06-26 — **Post-phase user-reported fixes** (see Decisions log 2026-06-26): overlay
editor reversed-text bug fixed + edit-text font/size matching; AI provider runtime toggle (Local Ollama
↔ Gemini) replacing the static env choice; Word export now recovers partial dropped words. All gates
green (Pest 141/1, pytest 85, larastan 0, Pint, ruff+black, build). — Prior: **Phase 7 done (Polish, tests, deploy, portfolio)**. Portfolio **landing
page** (replaces the starter welcome) + a useful **signed-in dashboard** (stat cards + recent docs via
`DashboardController`); branding switched to **"PDF Studio"** (config-driven), starter-kit sidebar links
removed. **Docker** stack written (NOT build-verified — Docker isn't installed here): root `Dockerfile`
(PHP-FPM + nginx + supervisor, self-contained), `pdf-service/Dockerfile` (py3.12), `docker-compose.yml`
(db `pgvector/pgvector:pg18` + app + queue + pdf-service), `docker/` configs + entrypoint, `.dockerignore`s,
env wiring in `.env.example`. **Demo seeder** (`DemoDocumentsSeeder`) + 3 committed sample PDFs/thumbnails
(`database/seeders/samples/`, generated by `generate_samples.py`); seeds `demo@example.com` / `password`.
**Portfolio `README.md`** (mermaid arch diagram, fidelity + smart-export + RAG stories, runbook, decisions)
+ `docs/screenshots/` capture checklist (images NOT committed — user captures them). Gates green: **Pest
138 pass/1 skip, pytest 79, larastan(L7) 0, Pint, ruff+black, `npm run build`**. ruff installed into venv.
Prev (Phase 6): AI Assistant panel (Chat RAG + Summarize + Translate); Claude `claude-opus-4-8`; JSON
embeddings + in-process cosine. Prev (Phase 5): smart Word export — `/pdf/ocr` + `/pdf/export/docx`._

---

## Phase board

| # | Phase | Status | Owner/session | Notes |
|---|---|---|---|---|
| 0 | Foundation & scaffolding | ✅ | Session 2026-06-13 | Both services wired; health E2E; tests green; initial commit |
| 1 | Document management | ✅ | Session 2026-06-15 | Upload→store→/pdf/info; library (rename/delete/download); PDF.js viewer; DocumentPolicy; all gates green |
| 2 | Page operations | ✅ | Session 2026-06-16 | `/pdf/pages` (organize/split/merge, 100% fidelity); versions model + restore; page-manager UI (native DnD); split/merge flows; all gates green |
| 3 | Overlay editor | ✅ | Session 2026-06-17 | Overlay CRUD + autosave + undo/redo; client-side Alpine/DOM editor over PDF.js; `/pdf/bake` (all visual types, rotation-aware, Golden Rule); bake→version; reviewed/corrected, all gates green |
| 4 | Forms & signatures | ✅ | Session 2026-06-18 | AcroForm detect + fill/flatten; `form_field`/`signature` overlays bake; reusable `signatures` table; signature pad (draw/type/upload) + form-fill UI; all gates green |
| 5 | Smart Word export | ✅ | Session 2026-06-20 | `/pdf/ocr` + `/pdf/export/docx` (smart native-vs-scan); OCR via PyMuPDF's **bundled** Tesseract (no system Tesseract/Ghostscript — only tessdata); pdf2docx for native, OCR-text→docx for scans; async `export_jobs` + job + viewer modal; all gates green |
| 6 | AI Assistant + OCR | ✅ | Session 2026-06-22 | "AI Assistant" panel: Chat (RAG) + Summarize + Translate, native & scanned; Claude `claude-opus-4-8` via Python `/ai/*`; JSON embeddings + in-process cosine (no pgvector); rate-limited; mocked-LLM tests; all gates green |
| 7 | Polish, tests, deploy | ✅ | Session 2026-06-22 | Portfolio landing + dashboard; "PDF Studio" branding; Docker stack (written, NOT build-verified — no Docker locally); demo seeder + sample PDFs; portfolio README + screenshots checklist; all gates green (Pest 138/1, pytest 79, larastan 0, Pint, ruff+black, build) |

---

## Shared infrastructure status

Tick these as they come into existence so phases don't rebuild them.

- ✅ Git initialized (initial commit on `master`)
- ✅ Python `pdf-service/` skeleton runs (`GET /health`, secret-protected)
- ✅ `PdfServiceClient` (Laravel → Python) exists (`health()`, `isHealthy()`)
- ✅ `documents` + `document_versions` migrations + models + factories (+ enums)
- ✅ Storage disk for PDFs configured (`pdfs` disk, private)
- ✅ Queue driver configured for async jobs (`database`, from starter kit)
- ✅ PDF.js viewer mounted in the frontend (`resources/js/pdf-editor/`, `pdfjs-dist` v6)
- ✅ `/pdf/pages` endpoint (organize/split/merge, lossless) + `PdfServiceClient::pages()`
- ✅ Versioned outputs: `document_versions` (+`page_count`/`size_bytes`) + `PageOperationService`;
  viewer renders the **active version** (latest, else original); restore is append-only
- ✅ Overlay JSON schema finalized (`document_overlays`: `type` enum + `payload` JSON in PDF
  user space, `z_index`/`order` paint sequence) — see Phase 3 Handoff for per-type payload shapes
- ✅ `/pdf/bake` endpoint (overlay → flattened PDF; rotation-aware, original bytes preserved) +
  `PdfServiceClient::bake()` + `PageOperationService::bake()` (→ new version, then clears overlays)
- ✅ AcroForm: `/pdf/form-fields` (detect) + `/pdf/form-fields/fill` (+`flatten`) +
  `PdfServiceClient::formFields()/fillFormFields()`; `form_field`/`signature` overlay types bake
- ✅ Reusable signatures: `signatures` table + `Signature` model/factory + `SignatureType` enum +
  `User::signatures()` (image stored inline as a data URL; owner-scoped)
- ✅ Text-extraction + OCR pipeline (shared by Word export & AI): `/pdf/ocr` (PyMuPDF's bundled
  Tesseract → searchable PDF + recognized `text`, cached by content hash) + `PdfServiceClient::ocr()`;
  smart `/pdf/export/docx` + `PdfServiceClient::exportDocx()`; async `export_jobs` + `ExportDocumentJob`
  + `ExportService`. Phase 6's `/pdf/extract-text` reuses `/pdf/ocr` for the scanned fallback (no system
  Tesseract needed — only a `tessdata/<lang>.traineddata` file)
- ✅ AI text extraction for RAG: `/pdf/extract-text` (native per-page text; OCR fallback for scanned/mixed
  via `/pdf/ocr`) + `PdfServiceClient::extractText()`
- ✅ **LLM provider configured = Anthropic Claude (`claude-opus-4-8`)** via Python `/ai/chat|summarize|translate`
  (`PdfServiceClient::chat()/summarize()/translate()`). Key (`ANTHROPIC_API_KEY`) lives **only** in
  `pdf-service/.env`. Internal streaming to Anthropic to dodge timeouts; full text returned to Laravel
- ✅ Embeddings + RAG vector store: `/ai/embed` (`PdfServiceClient::embed()`) with a deterministic local
  **`hash`** provider by default (Voyage AI optional); stored as **portable JSON vectors** on
  `document_chunks` and ranked **in-process by cosine** in `RagService` (pgvector NOT available on this
  Postgres — see decision below). `ChunkingService` + `RagService` (ingest/retrieve) + `AiAssistantService`
- ✅ AI data model: `ai_conversations` / `ai_messages` (+ `AiMessageRole` enum) / `document_chunks`
  (migrations + models + factories); `Document::chunks()` / `aiConversations()`
- ✅ Livewire `Documents\AiAssistant` side panel (Chat/Summarize/Translate, language picker, page-scope,
  sources, "AI may be wrong" notice) toggled from the viewer; per-user rate limit via `RateLimiter`
- ✅ Portfolio front: `welcome.blade.php` landing (Flux + @vite, dark-mode) + signed-in dashboard
  (`DashboardController` → stat cards + recent docs). Branding = **"PDF Studio"** via `config('app.name')`
  (set in `.env.example`); app-logo + sidebar de-starter-kitted
- ✅ Demo data: `DemoDocumentsSeeder` (idempotent, reads `database/seeders/samples/manifest.json`) +
  `DatabaseSeeder` creates `demo@example.com` / `password`. 3 committed sample PDFs + cover PNGs +
  `generate_samples.py` (PyMuPDF). Seeded files land on the `pdfs` disk
- ✅ Docker: root `Dockerfile` (multi-stage: composer→vendor, node→assets, php:8.4-fpm + nginx + supervisor),
  `pdf-service/Dockerfile` (python:3.12-slim), `docker-compose.yml` (db `pgvector/pgvector:pg18` + app +
  queue + pdf-service; shared `app-storage` volume for `storage/app`), `docker/{nginx.conf,php.ini,
  supervisord.conf,entrypoint.sh}`, root + service `.dockerignore`. **Written, not build-verified** (Docker
  not installed on the dev box). app & queue share the same image; queue overrides `command`
- ✅ Portfolio `README.md` (root) + `docs/screenshots/README.md` capture checklist. Python lint = ruff
  (added to the venv) + black, both clean and config'd in `pdf-service/pyproject.toml`

---

## Decisions log

Record any resolved "open decision" here with date + rationale.

- 2026-06-26: **Post-phase fixes (user-reported).** (1) **Reversed text-entry bug** in the overlay
  editor: the editable text `<div>` bound its content with `x-text="overlay.payload.text"`, so each
  keystroke's `input` → `payload.text` write made Alpine rewrite the node and snap the caret to the
  start (typing "nama" produced "aman"; backspace dead). Fixed by writing the initial content **once**
  via `x-init` and only re-syncing the DOM for *unfocused* programmatic changes via a guarded
  `x-effect` (undo/redo) — the contenteditable is now the source of truth while focused
  (`editor.blade.php`). (2) **Edit-text precision**: `editTextAt` now copies the source run's real
  metrics from PDF.js (`content.styles[fontName]` ascent/descent + generic family sans/serif/mono,
  plus bold/italic hints) onto the overlay, and the bake honors a new `font` field by selecting the
  matching **Base-14 family** (Helvetica/Times/Courier) instead of always Helvetica (`editor.js`,
  `pdf_bake.py` `_FONT_FAMILIES`). Edited text now keeps the original's serif look + size.
  (3) **AI provider is now a runtime toggle** (panel chip: **Local (Ollama)** vs **Gemini**), not a
  static `AI_PROVIDER` env. The `/ai/*` requests carry an optional `provider`; Python `_complete`
  dispatches on it (falling back to `AI_PROVIDER`) and reports the right model; Laravel threads it
  through `PdfServiceClient`/`AiAssistantService`/`AiAssistant` (re-validated server-side, remembered
  in the session). `config('services.ai.providers')` drives the toggle + footer label (display-only;
  real models live in `pdf-service/.env`). Default = **ollama** (offline, no key); flip to Gemini for
  higher quality. (4) **Word export dropped words**: `_recover_dropped_text` recovered only lines that
  were ≥60% absent, so partial drops (a few words pdf2docx loses under the watermark) slipped through.
  It now appends any source line containing **any** missing word (≥2 chars) to the "Recovered text"
  appendix — completeness over avoiding minor duplication. (5) **Dev-mode PDF render fix** (found while verifying in-browser): under `npm run dev`,
  Vite injects `import "/@vite/client"` into the served `pdf.worker.min.mjs`, which cannot resolve
  inside the Blob module worker, so the viewer/editor showed "We could not display this document".
  `ensurePdfWorker` now uses the worker URL directly when `import.meta.env.DEV` and keeps the MIME-safe
  Blob path for production builds only (`worker.js`). Browser-verified: PDF renders, forward typing +
  backspace work, and the AI panel provider toggle (Local Ollama default) answers grounded with page
  sources. (6) **Edit-text round 2** (user follow-up): "Edit existing text" made the word *vanish* —
  the run's real ascent+descent can be < 1em, so the editable box was ~11px while a 12px line is ~15px,
  and `overflow-hidden` + `whitespace-pre-wrap` clipped (and wrapped) it to nothing. Now the editable
  box grows DOWNWARD to ≥ `font_size * 1.4` while its TOP stays at the original cap line (baseline +
  ascent) so the **bake position is unchanged**; the contenteditable is `overflow-visible whitespace-pre`
  (one line, never clipped) (`editor.js` `editTextAt`, `editor.blade.php`). Added a **font-family picker**
  (Sans/Serif/Mono) to the text Properties panel (color/size/bold/italic/align/opacity were already
  there). **Translate now offers English** (was missing) and defaults to it (`ai-assistant.blade.php`,
  `AiAssistant::$targetLanguage`). Browser-verified: editing a serif line shows it on-screen in serif,
  the panel shows the auto-detected font, and Translate lists English first. Gates: **Pest 141 pass/1 skip, pytest 85,
  larastan(L7) 0, Pint, ruff+black, `npm run build`** all green. Live AI needs a running Ollama daemon
  and/or `GEMINI_API_KEY` in `pdf-service/.env`.

- 2026-06-22: **Phase 7 polish/deploy/portfolio.** **App image = one self-contained container** running
  **PHP-FPM + nginx + supervisor** (not separate php/nginx services sharing a volume) — simplest mental model
  for a beginner runbook, single port 8080, and the **queue worker reuses the same image** with a `command`
  override. Multi-stage build resolves Composer **vendor first** because the Vite/Tailwind build imports
  `vendor/livewire/flux/dist/flux.css` and scans Flux stubs (frontend stage would fail without vendor).
  **Docker files are written but NOT build-verified** — Docker Desktop isn't installed on the dev box (user's
  manual step per the plan's §B); the compose YAML parses and the layering is correct, but `docker compose
  build/up` must be run by the user to confirm. **DB image = `pgvector/pgvector:pg18`** so the in-process
  cosine RAG store can later upgrade to pgvector without changing the base image. **Branding = "PDF Studio"**
  (config-driven via `config('app.name')`, default set in `.env.example`) — rename by editing `APP_NAME`.
  **Dashboard stats use query aggregates** (`count()`/`sum()`/`has('versions')`), not a loaded collection
  with `$doc->latestVersion?->page_count` — larastan types the `hasOne()->ofMany()` relation as non-null, so
  the nullsafe tripped `nullsafe.neverNull`; aggregates are also cheaper. "Pages" reflects original
  `page_count` (not post-edit active counts) — fine for a headline stat. **Demo seeder is offline-safe**:
  sample PDFs + cover thumbnails are pre-generated and committed (no pdf-service call at seed time); re-run
  `generate_samples.py` only to refresh them. Demo login: **`demo@example.com` / `password`**. **Screenshots
  are NOT committed** — `docs/screenshots/README.md` lists the exact shots to capture from a seeded instance.
  **Deferred to the user:** install Docker + verify the stack, capture screenshots, pick/point a host, set
  production secrets (`APP_KEY`, real `PDF_SERVICE_SECRET`, DB password, optional `ANTHROPIC_API_KEY`).
- 2026-06-22: **Phase 6 AI Assistant.** **LLM = Anthropic Claude `claude-opus-4-8`** (project default;
  ids confirmed via the `claude-api` skill). **All model calls live in the Python service** under `/ai/*`
  (ARCHITECTURE §1 places "LLM + embeddings glue" there): keeps every provider key in `pdf-service/.env`
  (never in Laravel/browser) and keeps Laravel free of new Composer deps (added `anthropic` to the
  service's `requirements.txt`). Laravel orchestrates (auth, rate-limit, persistence, retrieval over its
  own DB) and talks to Python via `PdfServiceClient`. The Anthropic call **streams internally**
  (`messages.stream().get_final_message()`) to avoid HTTP timeouts but returns the **whole** answer — the
  Laravel/Livewire contract stays request/response (true end-to-end streaming deferred; the panel shows a
  "Thinking…" state). Adaptive thinking on. **Vector store: pgvector was the lead but is NOT installed on
  the dev/CI Postgres** (`pg_available_extensions` has no `vector`, and installing it on Windows/Postgres 18
  is non-trivial), so per the plan's allowed fallback we store embeddings as **portable JSON float arrays**
  on `document_chunks` and rank **in-process by cosine** in `RagService` (documents are owner-scoped &
  bounded, so this is cheap). Upgrade path documented in the migration. **Embeddings: provider-abstracted in
  Python (`AI_EMBEDDING_PROVIDER`)** — default **`hash`** (deterministic signed feature-hashing, no key,
  offline, dim 256) so the app + tests run with no embedding key; **Voyage AI** (Anthropic's recommended
  partner) is the real-semantic option. **`/pdf/extract-text`** does native per-page extraction and falls
  back to `/pdf/ocr` for scanned/mixed (graceful degrade to native text when tessdata is absent).
  **AI output stays in the panel — never written onto the PDF** (fidelity note). **Guardrails:** per-user
  `RateLimiter` (`AI_RATE_LIMIT_PER_MINUTE`=15), `AI_MAX_INPUT_CHARS` cap for summarize/translate, all
  service calls wrapped in try/catch → friendly errors. **Tests mock the LLM** (Laravel `Http::fake` of the
  Python endpoints incl. a deterministic embed closure; pytest fakes `ai_llm._complete`). Data extraction
  (stretch) NOT built. **Live AI needs `ANTHROPIC_API_KEY` in `pdf-service/.env`.**
- 2026-06-13: Architecture = Laravel + Python FastAPI microservice. (User confirmed.)
- 2026-06-13: Core editing uses the **overlay / non-destructive** approach. (Golden Rule.)
- 2026-06-13: AI scope = one "AI Assistant" panel (Chat + Summarize + Translate) + OCR;
  data extraction is a stretch goal. (User confirmed.)
- 2026-06-13: Word export = headline feature with smart native-vs-scan pipeline,
  positioned as best-effort. (User confirmed.)
- 2026-06-13: Dev environment verified — Git 2.54, PHP 8.4.22 + Composer (Herd, PowerShell
  only), Node 26 / npm 11, Python 3.14.5. Python deps via `requirements.txt` + venv. PyMuPDF
  + FastAPI confirmed installable on 3.14. Full setup guide in `phase-0-foundation.md` §A–F.
- 2026-06-13: `/health` is secret-protected (matches the "401 on missing secret" test). Add a
  separate open endpoint later only if an unauthenticated liveness probe is ever needed.
- 2026-06-13: `document_versions` migration timestamp bumped (+1s) so its FK to `documents`
  resolves — both were generated with the same timestamp and alphabetical order ran versions
  first. Source/status modeled as PHP enums (`DocumentSourceType`, `DocumentStatus`).
- 2026-06-14: **Primary DB = PostgreSQL 18** (switched from SQLite). Dev DB `pdf_editor`,
  test DB `pdf_editor_test` (phpunit points here for prod parity — full suite green on pg).
  Chosen over MySQL for pgvector (Phase 6 RAG) + stronger JSON. PHP Herd already ships
  `pdo_pgsql`; switch was config-only (migrations are DB-agnostic). Local password is dev-only.
- 2026-06-15: **`source_type` heuristic** = a page is "native" if it yields ≥16 non-whitespace
  chars of extractable text; all-native ⇒ `native`, none ⇒ `scanned`, otherwise ⇒ `mixed`,
  0 pages ⇒ `unknown` (`pdf-service/app/services/pdf_info.py`, `TEXT_CHAR_THRESHOLD`).
- 2026-06-15: **Upload limits** live in `services.pdf` (`max_upload_mb`=25, `max_pages`=500,
  env-overridable). `AppServiceProvider::configureFileUploads()` raises Livewire's global
  temporary-upload cap to match (default was 12 MB); the upload component applies the precise
  mime/size/page rules. `python-multipart` added to the service for multipart `File`/`Form`.
- 2026-06-15: **Thumbnails** — cover only (page 1 @ 96 dpi) is generated server-side on upload
  and stored on the private `pdfs` disk under `thumbnails/<uuid>.png`, served via the
  policy-protected `documents.thumbnail` route. The viewer's per-page rail is rendered
  **client-side by PDF.js** (no server round-trip). Original bytes are streamed as-is (Golden Rule).
- 2026-06-16: **`/pdf/pages` spec** uses 3 ops (not 5): **`organize`** unifies reorder + rotate +
  delete (a page list `[{source,rotate}]`; delete = omit, reorder = list order, rotate = 90°
  delta on the page's current rotation), **`split`** (`ranges` XOR `every`) → many files, **`merge`**
  (≥2 files, in order) → one. Multipart: repeatable `files` part(s) + a JSON `spec` form field.
  Response `{outputs:[{page_count,content_base64}]}`. PyMuPDF `insert_pdf`/`set_rotation` only —
  pages are copied, never re-rendered (Golden Rule). Discrete reorder/rotate/delete ops were
  intentionally folded into `organize` (the plan's enum was illustrative; one primitive = one
  page-manager "Save" + fewer round-trips).
- 2026-06-16: **Versioning model.** Each organize/restore writes a `document_versions` row
  (append-only, `version_number = max+1`); split/merge write new `documents`. Originals are never
  mutated. The **active bytes** = latest version, else original; `Document::activePath()` /
  `activePageCount()` expose it (query via `value()` to stay correctly nullable under larastan).
  **Restore = append a copy** of an old version as the new latest (non-destructive, no pointer
  column). Viewer/page-manager load the active URL; the library shows the active page count.
- 2026-06-16: **Drag-and-drop = native HTML5 DnD in Alpine** (`resources/js/pdf-editor/
  page-manager.js`), not `wire:sort` or SortableJS. Thumbnails render client-side with PDF.js
  inside `wire:ignore`, so Livewire-DOM sorting doesn't fit; native DnD avoids a new dependency
  (CLAUDE.md: no dep changes without approval). The final page list is pushed to the Livewire
  `Organize::save()` action.
- 2026-06-17: **Overlay editor = client-side Alpine/DOM over PDF.js** (no Fabric.js/canvas-lib
  dependency — mirrors the Phase 2 page-manager's native approach; CLAUDE.md: no deps without
  approval). Geometry stored in **PDF user space** (points, bottom-left); the JS uses PDF.js's
  own `viewport.convertToPdfPoint`/`convertToViewportPoint` (`resources/js/pdf-editor/coords.js`)
  so placement is zoom-independent. The **whole overlay list is the source of truth**: autosave
  (`Editor::syncOverlays`) swaps it atomically (delete-all + `createMany` in a transaction);
  client-side undo/redo via JSON snapshots.
- 2026-06-17: **Bake rotation handling** — for a page with `/Rotate ≠ 0`, `pdf_bake` temporarily
  `set_rotation(0)`, draws every overlay in plain unrotated mediabox space (flipping y against
  the unrotated height), then restores the original rotation. This sidesteps PyMuPDF's
  per-method rotation ambiguity entirely and matches PDF.js's unrotated `convertToPdfPoint`
  output. Locked by `test_whiteout_on_a_rotated_page_lands_in_user_space`.
- 2026-06-18: **Phase 4 forms & signatures.** AcroForm **fill = `form_field` overlays flattened by
  the existing `/pdf/bake`** (additive, original form untouched — the "flatten via existing bake"
  path; Golden Rule). `/pdf/form-fields` is detection-only; a separate `/pdf/form-fields/fill`
  (PyMuPDF `widget.update()` + optional `Document.bake(widgets=True)`) is the real interactive/
  flattened AcroForm primitive — tested + on the client, but the **editor UI uses overlay-bake**,
  not this. Form values bake with `insert_text` (baseline + `get_text_length` alignment), not
  `insert_textbox` (which writes nothing when a tight field-box overflows by a hair). **Signatures
  bake as images**: draw/type/upload all rasterize to a PNG **client-side**, so no server font is
  needed; stored **inline as a data URL** in a per-user `signatures` table (≤20, ≤5M chars). The
  signature pad + form-fill UI are **pure Alpine** inside `wire:ignore`; saved-signature CRUD
  returns the fresh list and the editor swaps its local state from the return value. **PKI/digital
  signing deliberately deferred** (out of scope). Detect rects come back in bottom-left user space
  so the editor reuses `coords.js`. The Phase 3 "reserved types skipped" bake test became
  "unknown types skipped".
- 2026-06-20: **Phase 5 smart Word export.** **OCR engine = PyMuPDF's *bundled* Tesseract**
  (`Pixmap.pdfocr_tobytes`), chosen over **ocrmypdf** (dropped): MuPDF's PyPI wheel ships Tesseract
  compiled in, so OCR needs **no system Tesseract or Ghostscript** — only the language data file
  (`tessdata/<lang>.traineddata`, ~4 MB, **gitignored**, one-line download in `tessdata/README.md`;
  path via `PDF_TESSDATA_PREFIX`, default `pdf-service/tessdata`). Verified working on Python 3.14;
  `pdf2docx` + numpy/opencv-headless/lxml all have cp314 wheels. **`/pdf/ocr`** → searchable PDF
  (image + invisible text layer) + recognized `text`, **idempotent, cached by sha256(bytes+lang)**
  under `.ocr_cache/` (gitignored). **DOCX strategy is hybrid**: native → `pdf2docx` (layout-aware);
  scanned/mixed → OCR first, then **build the DOCX from the OCR text blocks via python-docx** — because
  pdf2docx *refuses* a scanned/invisible-text PDF ("Words count: 0 … not supported"), so running it on
  an OCR'd PDF yields an empty file. This is exactly the edge over naive converters. **`engine`** on the
  job row = `pdf2docx` (native) or `ocr+python-docx` (scanned). **LibreOffice second engine: SKIPPED** (heavy, Windows-hostile;
  the `engine` column leaves room to add it later). Export is **async**: `export_jobs` table + `ExportJob`
  model/enum (`ExportJobStatus`, `ExportFormat`) + `ExportDocumentJob` (queued, `tries=1`, `timeout=600`,
  `failed()` safety net) + `ExportService` (queued→processing→completed/failed). Heavy calls use a
  separate `services.pdf.export_timeout` (300s) via a `longTimeout` on `PdfServiceClient`. Result `.docx`
  stored on the document's disk under `exports/<uuid>.docx`; download route is document-scoped
  (`documents.exports.download`, `can:view`, scope-bound, 404 until completed). Viewer has an "Export to
  Word" modal that `wire:poll`s while pending and reveals the download; **honest best-effort copy**
  (text + rough layout, not pixel-perfect). **Mixed PDFs are OCR'd wholesale** (native pages re-OCR'd) —
  acceptable for now, noted as a future refinement.
- 2026-06-17: **Phase 3 review/correction pass** (redone with the larger model). The earlier pass
  was functional + tested but left the gate red: 2 larastan(L7) `list` vs `array` errors
  (`Editor::overlays()`, `PageOperationService::bake()`) — fixed with `array_values(...)`. Also
  fixed an **image-insert off-page bug** (placement referenced the PDF origin = bottom-left
  instead of the screen top-left, dropping new images below the page) and added the rotation
  bake test. Re-confirmed Golden Rule: bake draws on an in-memory copy; the original on disk is
  byte-identical (asserted in `OverlayBakeTest`).
- 2026-06-15: Cleaned **latent Phase 0 debt** so `composer test` is fully green: model
  `@property` used a non-existent `Illuminate\Support\CarbonImmutable` (→ `Carbon\CarbonImmutable`),
  redundant `??` offsets, `UserFactory::withTwoFactor()` had an empty body (now sets 2FA columns),
  and repo-wide Pint EOF/unused-import fixes. larastan runs at **level 7** (use `--memory-limit=1G`).

---

## Risks / watch-list

- True in-place text editing is hard (font subsetting, no reflow). Default to overlay;
  treat deep text edit as stretch.
- PDF→Word is inherently lossy for complex docs. Manage expectations in the UI copy.
- Untrusted PDF input is a security surface — isolate processing in the Python service.
