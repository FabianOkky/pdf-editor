"""PDF Editor — Python microservice (FastAPI).

Internal service for PDF processing (PyMuPDF), OCR, Word export, and AI glue.
Called by the Laravel app over localhost REST with a shared-secret header.
"""
